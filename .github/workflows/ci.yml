name: CI
on: [pull_request, push]
jobs:
 mix_test:
   name: mix test (Elixir ${{ matrix.elixir }} OTP ${{ matrix.otp }})
   strategy:
     matrix:
       elixir: ['1.5.x', '1.6.x', '1.7.x', '1.8.x', '1.9.x']
       include:
         - elixir: '1.5.x'
           otp: '19.x'
         - elixir: '1.6.x'
           otp: '20.3.6'
         - elixir: '1.7.x'
           otp: '21.x'
         - elixir: '1.8.x'
           otp: '22.x'
         - elixir: '1.9.x'
           otp: '22.x'
   runs-on: ubuntu-16.04
   steps:
     - uses: actions/checkout@v1
     - uses: actions/setup-elixir@v1.0.0
       with:
         otp-version: ${{ matrix.otp }}
         elixir-version: ${{ matrix.elixir }}
     - name: Install Dependencies
       run: mix deps.get
     - name: Run Tests
       run: mix test
